apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

jar {
    manifest {
        attributes(
                'Main-Class': 'eu.rekisoft.android.Scanner'
        )
    }
}
sourceCompatibility = "1.8"
targetCompatibility = "1.8"
